import sys
sys.stdin = open("최대상금_input.txt")

def search(n, k):
    if n==k:
        

for tc in range(int(input())):
    num, c = input().split()
    num = list(map(str, num))
    A = [i for i in range(len(num))]
    c = int(c)
    arr = [[0]*720 for _ in range(c)]

